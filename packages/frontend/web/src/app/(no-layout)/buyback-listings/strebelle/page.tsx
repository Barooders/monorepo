import StrebelleListing from '@/components/pages/BuyBackListing/strebelle';

export const metadata = {
  robots: {
    index: false,
    follow: false,
  },
};

export default function StrebelleListingPage() {
  return <StrebelleListing />;
}
