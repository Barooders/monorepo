meta {
  name: Get families
  type: http
  seq: 4
}

get {
  url: {{strapiBaseUrl}}/api/pim-product-families?populate=productType&fields[0]=name&filters[name][$eq]=Racemax
  body: none
  auth: none
}

query {
  populate: productType
  fields[0]: name
  filters[name][$eq]: Racemax
}
