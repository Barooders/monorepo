table:
  name: SearchAlert
  schema: public
object_relationships:
  - name: SavedSearch
    using:
      foreign_key_constraint_on: searchId
select_permissions:
  - role: registered_user
    permission:
      columns:
        - id
        - isActive
        - latestRunAt
        - latestResultsCount
        - searchId
      filter:
        SavedSearch:
          customerId:
            _eq: X-Hasura-User-Id
